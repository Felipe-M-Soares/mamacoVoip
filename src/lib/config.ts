// Troque essa URL quando publicar os releases do app desktop no GitHub
// (depois de rodar `npm run electron:build` e subir os artefatos, ou
// configurar CI pra fazer isso automaticamente). Até lá, aponta pro
// repositório — a pessoa consegue ver as instruções de build lá.
export const DESKTOP_DOWNLOAD_URL = 'https://github.com/SEU_USUARIO_GITHUB/SEU_REPOSITORIO/releases/latest'
